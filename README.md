
# Order Book Engine

Lightweight matching engine that simulates a limit order book for testing trading strategies and pricing logic.

## Setup

1. `cmake -S . -B build`
2. `cmake --build build`
3. `ctest --test-dir build`
